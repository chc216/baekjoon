#include <iostream>
#include <algorithm>
#include <string>
#include <vector>

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int T;
    cin >> T;
    while(T--)
    {
        int N;
        cin >> N;
        string txt;
        cin >> txt;

        vector<char> a;
        vector<char> b;

        if(N == 1)
        {
            cout << "Yes";
        }

        for(int i = 0; i < N/2)
    }

    return 0;
}
